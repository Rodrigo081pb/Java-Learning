package Operadores;

public class OperadoresDeAtribuicao {
    public static void main(String[] args) {
        /*
            Operadores de atribuição

            += : somar e atribuir           | A += B = a + b
            -= : subtrair e atribuir        | A -= B = a - b
            *= : multiplicar e atribuir     | A *= B = a * b
            /= : Dividir e atribuir         | A /= B = a / b


        int x = 10;
        x += 10; 10 + 10
        System.out.println(x); 20


         É como se eu estivesse atribuindo literalmente um valor a uma váriável de acordo
         com a atribuição da operação que eu estou fazendo


        // Multiplicação
        int y = 2;
        y *= 2; // 2 * 2 : 4
        System.out.println(y);

        // Divisão
        int z = 10;
        z /= 10;

        // Subtração
        int e = 10;
        e -= 10;
        System.out.println(e);

        */


    }
}
